singleuse:
  TeleportHome:
    lore: "Teleport to your bed"
    glint: true
    max-uses: 1
    config: "utility/TeleportHome.yml"

  AirStrike:
    lore: "Spawn TNT in the sky"
    glint: true
    max-uses: 1
    config: "utility/AirStrike.yml"

tools:
  BigHole:
    lore: "Digs a big hole"
    glint: true
    max-uses: 10
    config: "tools/BigHole.yml"

bows:
  ExplodingBow:
    lore: "Arrows explode on contact with anything"
    glint: true
    max-uses: 3
    config: "bows/ExplodingBow.yml"

shields:
  HeavyShield:
    lore: "Anything that hits you will be knocked back a lot"
    glint: false
    max-uses: 5
    config: "shields/HeavyShield.yml"